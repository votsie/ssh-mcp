"""
Точка входа: ``python -m ssh_mcp``.

Отдельный модуль, а не ``python -m ssh_mcp.server``, и это не стиль, а
необходимость. При запуске модуля через ``-m`` он выполняется под именем
``__main__``, а когда ``shell_tools`` и остальные делают ``from .server import
mcp``, тот же файл импортируется второй раз — уже как ``ssh_mcp.server``. В
результате возникают ДВА разных экземпляра ``MCPServer``: инструменты
регистрируются на одном, а запускается другой, и наружу выходит меньше
половины из них.
"""

from ssh_mcp.server import main

if __name__ == "__main__":
    main()
